{
  "name": "hello-next",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "next",
    "build": "next build",
    "export": "next export",
    "deploy": "rmdir /q /s .next && next build && next export && type nul > out/.nojekyll",
    "start": "next start",
    "startWithPort": "next start -p %PORT%"
  },
  "prettier": {
    "singleQuote": true
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "isomorphic-unfetch": "^3.0.0",
    "next": "9.1.2",
    "react": "16.11.0",
    "react-dom": "16.11.0"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^2.0.0",
    "cross-env": "^6.0.3",
    "prettier": "1.18.2"
  }
}
